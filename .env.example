# Cheie Alpha Vantage (opțională, pentru fallback / intraday complet)
ALPHA_VANTAGE_API_KEY=schimba-cu-cheia-ta

# Ordinea providerilor (fallback). Valori suportate: yahoo, alpha_vantage
MARKET_PROVIDER_ORDER=yahoo,alpha_vantage

# TTL cache (secunde) pentru quotes (reduce traficul/flicker)
MARKET_CACHE_TTL_SECONDS=5


APP_ENV=dev
APP_NAME=AI Stock Predictor v2
SQLALCHEMY_DATABASE_URI=sqlite:///./aibursa_v2.db
LOG_LEVEL=INFO

# Chei externe (exemple)
ALPHAVANTAGE_API_KEY=REPLACE_ME
NEWS_API_KEY=REPLACE_ME
